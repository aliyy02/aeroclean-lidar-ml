# Core
numpy>=1.24
scipy>=1.10
pyyaml>=6.0

# Sim API (install separately on the machine that talks to UE5)
# cosysairsim   # commented; install from the CosysAirSim repo

# Visualization / debugging
open3d>=0.18
matplotlib>=3.7

# Training (Phase 2 only — install on the training machine / Colab)
# torch>=2.1
# torch-geometric>=2.5
# wandb>=0.16
# scikit-learn>=1.3
