# Figure index — maps each file to its slide

All figures generated by `analysis/make_slides.py` (regenerate with
`python3 analysis/make_slides.py` from the repo root). Filenames are slide-numbered so
they bind to `DECK_PROMPT.md`. The color convention is constant:
**blue = glass, gold = frame, green = ground, red = interior.**

| File | Slide | What it shows |
|------|-------|---------------|
| `S03_ransac_baseline.png` | 3–4 | Single flat board → one RANSAC plane (works); real window facade → many depth layers (frame front / glass / interior behind) → single-plane assumption fails. |
| `S06_pointnet_motivation.png` | 6 | Why PointNet++: local "query balls" learn the density + geometric cues RANSAC hand-tuned. |
| `S07_testbed_schematic.png` | 7 | The 6-DOF LiDAR test bed: a known commanded pose gives ground-truth sensor position + window-corner geometry. *(pair with the real rig photo `S07_testbed_photo.png` you capture).* |
| `S08_pointcloud_explainer.png` | 8 | One real LiDAR frame colored by intensity + plain-language "what is a point cloud" panel. |
| `S10a_labels.png` | 10 | The labelled result on real scans (L6 / Oxy / Bech): glass / frame / interior / ground. |
| `S10b_class_distribution.png` | 10 | Per-point class mix per dataset (why glass is the minority class). |
| `S11_sim_to_real_gap.png` | 11 | Simulator raw geometry (every beam returns) vs real LiDAR on glass (sparse, saturated). |
| `S12a_angular_glass.png` | 12 | Median glass intensity vs incidence angle — L6 specular spike, Oxy flat-bright, Bech rising to 255 at grazing. |
| `S12b_saturation_hist.png` | 12 | Glass intensity histograms; fraction clipped at the 255 sensor ceiling per glass type. |
| `S12c_seethrough.png` | 12 | Top-down view: beams pass THROUGH glass and return from inside the room (interior, red, behind the glass plane). |
| `S13_forward_model.png` | 13 | Forward model: reflectance ρ(θ) (diffuse + grazing + specular burst) and return-probability P(θ). |
| `S14_buildings_gallery.png` | 14 | Procedural building generator (facade systems + footprints) used to synthesize sim training scenes. |
| `S16_results_placeholder.png` | 16 | Confusion-matrix + metrics template, marked PENDING INFERENCE (fill after running the model). |

**Capture yourself** (see `CAPTURE_LIST.md`): `S14_ue5_sim.png` (UE5 viewport),
`S07_testbed_photo.png` (rig photo), `S08_rviz_scan.png` (RViz, optional),
`S12_facade_photos.png` (the three real facades, optional).
