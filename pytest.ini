[pytest]
# The live ML pipeline is buildings/ (procedural generator + capture) and forward_model/
# (the first-principles LiDAR model). legacy/ holds the archived flat-facade pipeline and the
# retired GPULidar path -- reference only, not collected.
testpaths = buildings forward_model calibration
norecursedirs = legacy data .pytest_cache *.egg __pycache__
